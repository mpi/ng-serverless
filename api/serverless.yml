service:
  name: aws-nodejs-typescript

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack
  - serverless-offline

provider:
  name: aws
  runtime: nodejs6.10

functions:
  express:
    handler: handler.express
    events:
     - http:
         path: api/{proxy+}
         method: any
         cors: true